{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "npm",
            "script": "compile", // Убедитесь, что у вас есть этот скрипт в package.json
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$tsc"
            ],
            "label": "npm: compile",
            "detail": "tsc -p ./" // Это описание, которое вы увидите
        }
    ]
}